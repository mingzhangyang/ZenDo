{
  "name": "zen-do",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "cf:dev": "npm run build && npx wrangler dev --env local",
    "deploy": "npm run build && npx wrangler deploy --env production",
    "deploy:production": "npm run build && npx wrangler deploy --env production",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "lucide-react": "^1.8.0",
    "motion": "^12.23.24",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "tailwindcss": "^4.1.14",
    "typescript": "~6.0.3",
    "vite": "^8.0.8"
  }
}
